---
import Card from "@/components/ui/Card.astro";
import Button from "@/components/ui/Button.astro";
import Icon from "astro-iconify";
import TagButton from "./TagButton.astro";

const post = Astro.props.post;
---

<Card
    size="full"
    hover
    class="flex flex-col overflow-hidden p-0 border-border backdrop-blur-sm"
>
    {post.frontmatter.image?.url && (
        <div class="w-full h-44 sm:h-58 overflow-hidden shrink-0">
            <img
                src={post.frontmatter.image.url}
                alt={post.frontmatter.image.alt ?? post.frontmatter.title}
                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
            />
        </div>
    )}
    <div class="flex flex-col flex-1 p-4 sm:p-5 gap-3">
        <div class="flex flex-wrap gap-2">
            {post.frontmatter.tags?.slice(0, 2).map((tag: string) => (
                <TagButton tag={tag} />
            ))}
        </div>
        <h3 class="text-base sm:text-lg font-bold leading-snug group-hover:text-chart-1 transition-colors line-clamp-2">
            {post.frontmatter.title}
        </h3>
        <p class="text-muted-foreground text-sm leading-relaxed line-clamp-3 flex-1">
            // {post.frontmatter.description}
        </p>
    </div>
    <div class="flex items-center justify-between px-4 sm:px-5 py-3 border-t border-border mt-auto">
        <div class="flex flex-col gap-1.5">
                <span class="text-xs text-muted-foreground flex items-center gap-1">
                    <Icon name="mdi:calendar-month-outline" class="w-3.5 h-3.5 inline-block" />
                    {new Date(post.frontmatter.pubDate).toLocaleDateString('pt-BR', { day: '2-digit', month: 'long', year: 'numeric' })}
                </span>
                <span class="text-xs text-muted-foreground flex items-center gap-1">
                    <Icon name="lucide:clock" class="w-3.5 h-3.5 inline-block" />
                    {post.frontmatter.readingTime} min de leitura
                </span>
            </div>
        <Button
            href={post.url}
            variant="outline"
            class="hover:border-chart-1/50 shrink-0 group"
        >
            <span>Ler mais</span>
            <Icon name="mdi:arrow-right" class="w-4 h-4 group-hover:translate-x-1 transition" />
        </Button>
    </div>
</Card>
